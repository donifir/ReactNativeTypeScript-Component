import { Image, StyleSheet, Text, View } from 'react-native'
import React from 'react'

const IconTabComponent = () => {
  return (
    <View>
      <Image source={require('./../assets/icon/home.png')} />
    </View>
  )
}

export default IconTabComponent

const styles = StyleSheet.create({})